# Path to the Python interpreter in the mcp_server virtualenv
# Run `cd ../mcp_server && poetry env info --executable` to find it
SERVER_PYTHON=/path/to/mcp_server/venv/bin/python

# Module name for the MCP server (default: backlight_mcp)
SERVER_MODULE=backlight_mcp
